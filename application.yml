spring:
    datasource: 
        url: jdbc:mysql://127.0.0.1:3306/springJpaExample
        username: root
        driver-class-name: org.mariadb.jdbc.Driver
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5InnoDBDialect

